Interviewer Questions List
🔹 Introduction

Introduce yourself.

🔹 AWS – EC2 & Scaling

If your application is running on an EC2 instance and experiencing high traffic spikes during business hours, how will you handle this situation?
If your EC2 instance crashes unexpectedly, what troubleshooting approach will you follow?
If your EC2 instance needs to download something from the internet but is unable to, what could be the possible causes?

🔹 AWS – S3

If someone accidentally deletes data from S3, how will you restore it?

🔹 AWS – Networking

How will you connect your on-prem data center with AWS cloud?
If you have two VPCs (frontend in one VPC and backend in another) and they are not communicating,
What could be the issues?
How will you fix it?
If your EC2 application needs to fetch objects from an S3 bucket but cannot,
How will you enable communication between EC2 and S3?

🔹 Kubernetes

Explain the architecture of Kubernetes.
How will you upgrade an EKS cluster including worker nodes?
If you want 10 pods to be scheduled equally across all nodes (for Prometheus/Filebeat type setup),
How will you ensure this distribution?
If your application is running in a pod and you want it accessible via a domain (e.g., ringi.com),
How will you configure the complete setup?
(Ingress, Service, Load Balancer, Route53 flow explanation)

🔹 Terraform

If you need to create 10 EC2 instances with the same configuration,
What will you use in Terraform?
If a VPC already exists manually and you need to use it in Terraform,
How will you reference it?
If a resource created by Terraform is manually deleted and you run terraform apply again,
What will happen?
If you have separate Terraform modules for VPC and EC2 and need to pass output from one module to another,
How will you do it?

🔹 CI/CD – Jenkins

What stages have you written in your Jenkins pipeline?

🔹 Linux

If you are getting “Permission Denied” while running a script,
How will you troubleshoot it?
If your Linux server is performing very slow,
How will you troubleshoot it?
Which commands will you use?

📊 Interview Focus Analysis

This interview mainly focused on:

AWS fundamentals (EC2, VPC, S3, IAM, VPN, VPC Peering)

Kubernetes basics (Architecture, Ingress, DaemonSet, Pod scheduling)

Terraform (Modules, Data blocks, Count, Drift handling)

Linux troubleshooting (Performance, Permissions)

Basic Jenkins pipeline knowledge

This is a mid-level DevOps (3–5 years experience) type interview.
